spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/secure_notes
    username: postgres
    password: 1287985dbc

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080